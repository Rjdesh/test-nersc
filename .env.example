# Local prototype credentials for the Vite server middleware.
# Keep private auth values server-side: do not use the VITE_ prefix.
SFAPI_CLIENT_ID=
SFAPI_PRIVATE_KEY_JSON=
NERSC_USER_ID=rjdesh
NERSC_MACHINE_ID=perlmutter gpu
